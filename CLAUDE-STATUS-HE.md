# GreekCloud — מצב העבודה של קלוד

עודכן: 14 בספטמבר 2026. ענף: `claude/adoring-feynman-hff8a6`.

מסמך זה מחליף את נוהל הסנכרון הדו־צדדי שב־`CLAUDE-SYNC-HE.md`. לבקשת הבעלים,
הקוד של Codex נלקח כמות שהוא וקלוד ממשיך אותו לבדו. אין עוד שני סוכנים על אותו קוד.

## מה נלקח

ענף `codex/mobile-crm` (‏`b76bc39`) מוזג לענף העבודה במיזוג fast-forward, בלי קונפליקטים.
נכללים: תיקיית `crm/` המלאה, חמש המיגרציות, הגשר `lib/crm-sync.js` והשינוי ב־`api/submit.js`.

אימות בסיס לפני שינויים: `pnpm install` תקין, `pnpm typecheck` נקי, `pnpm build` עבר,
`pnpm test` — 29 עברו, 0 נכשלו, 2 בדיקות HTTP דולגו ללא שרת בדיקה.

## מה תוקן

כל שמונת הממצאים המאומתים ב־`QA-REPORT-2026-09-09-HE.md`. לכל תיקון נוספה בדיקת רגרסיה.

| מזהה | מה תוקן | קבצים | ראיה |
| --- | --- | --- | --- |
| QA-01 | הטיוטה שמרה דרכון, פרטי קשר והסכמות ללא תפוגה | `assets/intake-draft.js` (חדש), `assets/intake.js`, `assets/notice.js`, `assets/form.css`, שני קובצי intake | 5 בדיקות מול DOM מדומה |
| QA-02 | השרת לא אכף גיל, ערכי בחירה, תאריך וסוג קובץ | `api/submit.js` | 16 בדיקות עם ספקים מדומים |
| QA-03 | הטופס האנגלי ביקש דרכון בפורמט שהשרת דוחה | `en/intake.html`, `intake.html`, `assets/intake.js` | בדיקה בשתי השפות |
| QA-04 | כשל מייל שמוחזר כערך ולא כחריגה לא נרשם | `api/submit.js` | 3 בדיקות: הצלחה, `error`, חריגה |
| QA-05 | תפריט המובייל לא נסגר בניווט באותו עמוד | `assets/settings.js` | Chromium ‏390×844, לפני ואחרי |
| QA-06 | החיפוש לא מצא את מספר התיק המוצג | `components/crm/CrmApp.tsx` | 2 בדיקות |
| QA-07 | דוח ״7 ימים אחרונים״ לא הוגבל לשבוע | `lib/crm/weekly.ts` (חדש), `store.ts`, `CrmApp.tsx` | 6 בדיקות עם זמן קבוע |
| QA-08 | תזכורת טיסה נשארה אצל האחראי הקודם | `supabase/migrations/202609110006_reminder_ownership.sql` (חדש) | 5 בדיקות מול PostgreSQL מבודד |

### ראיות משמעותיות

**QA-05** שוחזר ותוקן בדפדפן אמיתי ברוחב 390. לפני: `aria-expanded=true`, גלילת הגוף
נעולה, הפאנל מכסה את המקטע, מיקוד ב־`BODY`. אחרי: `aria-expanded=false`, גלילה
משוחררת, הפאנל סגור, מיקוד על `#pricing`.

**QA-08** שוחזר במסד מבודד: בהסרת מיגרציה 006 שלוש מחמש הבדיקות נכשלות ומראות את
האחראי הקודם עדיין בתור למייל ולטלגרם. עם המיגרציה — התור מסומן
`recipient_no_longer_responsible` והתזכורת עוברת לאחראי החדש.

## מצב הבדיקות

`pnpm test` — **69 בדיקות, 67 עברו, 0 נכשלו, 2 דולגו** (בדיקות ה־HTTP, שדורשות
`CRM_TEST_ORIGIN` ושרת מקומי). `pnpm typecheck` נקי, `pnpm build` עובר.

בדיקות האתר רצות כעת באותה חבילה: `crm/package.json` מריץ גם `../tests/*.test.mjs`,
ו־`crm-checks.yml` מאזין גם ל־`assets/**` ול־`tests/**`.

## שינויי מבנה

- `assets/intake-draft.js` — מדיניות הטיוטה, מופרדת כדי שתהיה ניתנת לבדיקה.
- `crm/lib/crm/weekly.ts` — חלון שבעת הימים, מחוץ ל־`store.ts` שהוא `server-only`.
- `tests/` בשורש — בדיקות האתר.

## מה עדיין חסום, ולמה

החסמים מסעיף 6 בדוח ה־QA לא זזו, ואי אפשר להסיר אותם מכאן:

1. **אין Supabase מחובר.** נדרשים פרויקט מורשה, הרצת שש המיגרציות לפי סדר,
   והזמנת מנהל המערכת. הסיסמה נבחרת על ידי הבעלים.
2. **אין Turnstile.** בלי שני המפתחות הקליטה הציבורית חסומה ומחזירה 503. זו חסימה מכוונת.
3. **אין מייל/טלגרם מאומתים ואין cron פעיל.** תיקון QA-04 מתעד כעת כשל משלוח,
   אבל אימות אמיתי דורש חשבון ויעדי בדיקה מאושרים.
4. **בדיקת הרשאות בענן, גיבוי ושחזור, ו־MFA** — לא נבדקו. אין הבטחה לאפס חדירות.

## החלטות הבעלים (11 בספטמבר 2026)

1. **דרכונים — ישראלים בלבד, בינתיים.** אין שינוי: כלל 8 הספרות בשרת ובשני
   הטפסים נשאר כפי שהוא. אם וכאשר יתווספו יעדים/דרכונים זרים, זו תהיה החלטה
   נפרדת שדורשת גם הרחבת השרת וגם הטפסים יחד.
2. **מיזוג ל־`main` — רק כשהמערכת מוכנה להשתלב בפועל, לא לפני.** כלומר: לא
   כשלב טכני מוקדם, אלא אחרי שה־CRM מחובר בענן (Supabase, Turnstile, מייל/
   טלגרם), נפרס כפרויקט נפרד ועבר בדיקת קצה־לקצה אמיתית. עד אז השינויים
   נשארים בענף `claude/adoring-feynman-hff8a6` בלבד ולא נוגעים באתר החי.
   ראו "מה עדיין חסום" למטה לרשימת הצעדים שצריכים לקרות קודם.
3. **QA-09 (תצוגת סלפי מול CSP) — נסגר, לא לתקן.** הבעלים אינו רואה בזה בעיה
   שדורשת טיפול: גם אם התצוגה המקדימה לא תופיע בדפדפן מסוים, זה לא אמור
   להקשות על הלקוח או לחסום את השליחה. אין להרחיב את ה־CSP או להוסיף מורכבות
   בשביל זה.
4. **`noindex` באתר.** עדיין פתוח — צריך לברר אם זו בחירת טרום־השקה.

## הערה על תחזוקה

`components/crm/CrmApp.tsx` הוא 2,515 שורות בקומפוננטה אחת, ו־`lib/crm/store.ts`
הוא 797 שורות. זה עובד, אבל זו הנקודה שתכאב בתחזוקה. פירוק הדרגתי אפשרי
בלי כתיבה מחדש; לא ביצעתי אותו כדי לא לערבב שינוי מבנה בתיקוני באגים.

## בדיקה מקיפה + מיזוג main (14 בספטמבר 2026)

לבקשת הבעלים: בדיקה מלאה של הכנת ה־CRM למיזוג, כולל אבטחה, לפני חיבור עתידי לענן.

### תגלית מרכזית: `main` התרחק משמעותית תוך כדי העבודה

בזמן שעבדתי בענף נפרד, `main` התקדם ב־7 קומיטים: **האתר עלה לאוויר בפועל**
(מעבר לדומיין `greek-cloud.com`, הסרת `noindex`, עמודי תוכן חדשים, עמודים משפטיים
שעברו בדיקת עורך דין). בנוסף, **סשן קלוד נפרד לגמרי** ביצע בו־זמנית סקירת אבטחה
עצמאית על `api/submit.js` ו־`assets/intake.js` — בדיוק הקבצים שאני תיקנתי — ותיקן
בעיות דומות בדרך שונה. זה יצר קונפליקט אמיתי בקוד (לא רק בתוכן), שפתרתי בשילוב
מודע של הטוב משני הצדדים, ולא בבחירה עיוורת של צד אחד:

- **לקחתי מ־main:** `crypto.randomUUID()` במקום `Math.random()` למזהה הפנייה
  (המזהה האקראי הוא הדבר היחיד שמפריד בין תיק רפואי של אדם אחד למשנהו — `Math.random()`
  ניתן לניחוש חלקית מהגרלות קודמות, זו הייתה חולשה אמיתית בקוד שלי); שמות קבצים
  קבועים לפי הסיומת שזוהתה בפועל, בלי להשתמש בשם הקובץ שהמשתמש שלח (מונע כל השפעה
  שלו על מפתח האחסון); הגבלת קצב בסיסית; ניקוי תווי בקרה מהקלט; הסכמה נפרדת ומפורשת
  לעיבוד מידע רפואי (`c_health`).
- **שמרתי משלי:** רשימת ערים מותרות, בדיקת מצב מרשם מותר, ואימות תאריך הגעה תקין —
  none מזה היה קיים ב־main.
- **QA-01 (טיוטת הטופס):** main הוסיף תיקון עצמאי, אבל חלקי — הוא עדיין שמר שם מלא,
  טלפון, מייל וכל ההסכמות (הסיר רק דרכון ותיאור המצב). בחרתי בגרסה שלי, שמגבילה
  לשלושה שדות בלבד ולא שומרת שום פרט מזהה או הסכמה, כי היא זו שעונה במלואה על
  הממצא המקורי ב־QA-01.
- **תקלה שמצאתי ב־main עצמו, לא קשורה למיזוג:** תיבת הסכמה `c_health` הוגדרה חובה
  בשרת ובטופס העברי, אבל **חסרה לגמרי בטופס האנגלי** — כל שליחה באנגלית הייתה
  נדחית תמיד. הוספתי את התיבה החסרה עם ניסוח מקביל, ויישרתי גם תכונות `autocomplete`/
  `spellcheck` שהוחלו רק בעברית.

כל הקונפליקטים (`.gitignore`, `api/submit.js`, `assets/intake.js`, `intake.html`,
`en/intake.html`) נפתרו ונבדקו. `crm/` עצמה לא נגעה כלל ב־7 הקומיטים של `main` —
אפס קונפליקט שם.

### סקירת אבטחה — שתי בדיקות נפרדות

1. **סקירה על כל ה־diff מול `main`** (כל שינויי ה־CRM, הגשר, ומיגרציות ה־SQL):
   לא נמצאו ממצאי HIGH/MEDIUM. הקוד עוקב אחר תבניות בטוחות קיימות — סכמות zod
   קפדניות, פונקציות `security definer` נעולות עם `search_path`, מדיניות RLS
   מבוססת על תפקיד פעיל, השוואת טוקן קבועת־זמן לנקודות ה־cron/webhook, ואימות
   קבצים לפי בייטים אמיתיים.
2. **סקירה ממוקדת על `api/submit.js` המאוחד** אחרי המיזוג: נבדק במפורש שאין לוגיקה
   כפולה או מתה משאריות המיזוג, שאין דרך להגיע לכתיבה ל־Blob לפני שכל האימותים
   עברו, ושאין דליפת פרטי שגיאה פנימיים ללקוח. לא נמצאו ממצאים.

### אימות אחרי המיזוג

`pnpm test` באתר — 25/25 עברו. `pnpm test` ב־CRM — 69 (67 עברו, 2 דולגו כרגיל).
`pnpm typecheck` נקי, שני ה־build עברו (אתר סטטי + CRM). קומיט המיזוג נדחף לענף
`claude/adoring-feynman-hff8a6` (`e0be013`), **לא ל־`main`** — עדיין בהתאם להחלטת
הבעלים שלא למזג עד שה־CRM מוכן להשתלב בפועל.

### מה זה אומר להכנה לחיבור עתידי לענן

הענף עכשיו **מסונכרן עם המצב החי של האתר** ולא רק עם התמונה מלפני שבוע — כשיגיע
הרגע למזג בפועל, לא צפוי קונפליקט נוסף מהסוג הזה. רשימת החסמים לחיבור בפועל
(Supabase, Turnstile, מייל/טלגרם) לא השתנתה — ראו "מה עדיין חסום" למעלה.
