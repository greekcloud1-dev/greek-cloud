# GreekCloud — מצב העבודה של קלוד

עודכן: 11 בספטמבר 2026. ענף: `claude/adoring-feynman-hff8a6`.

מסמך זה מחליף את נוהל הסנכרון הדו־צדדי שב־`CLAUDE-SYNC-HE.md`. לבקשת הבעלים,
הקוד של Codex נלקח כמות שהוא וקלוד ממשיך אותו לבדו. אין עוד שני סוכנים על אותו קוד.

## מה נלקח

ענף `codex/mobile-crm` (‏`b76bc39`) מוזג לענף העבודה במיזוג fast-forward, בלי קונפליקטים.
נכללים: תיקיית `crm/` המלאה, חמש המיגרציות, הגשר `lib/crm-sync.js` והשינוי ב־`api/submit.js`.

אימות בסיס לפני שינויים: `pnpm install` תקין, `pnpm typecheck` נקי, `pnpm build` עבר,
`pnpm test` — 29 עברו, 0 נכשלו, 2 בדיקות HTTP דולגו ללא שרת בדיקה.

## מה תוקן

כל שמונת הממצאים המאומתים ב־`QA-REPORT-2026-09-09-HE.md`. לכל תיקון נוספה בדיקת רגרסיה.

| מזהה | מה תוקן | קבצים | ראיה |
| --- | --- | --- | --- |
| QA-01 | הטיוטה שמרה דרכון, פרטי קשר והסכמות ללא תפוגה | `assets/intake-draft.js` (חדש), `assets/intake.js`, `assets/notice.js`, `assets/form.css`, שני קובצי intake | 5 בדיקות מול DOM מדומה |
| QA-02 | השרת לא אכף גיל, ערכי בחירה, תאריך וסוג קובץ | `api/submit.js` | 16 בדיקות עם ספקים מדומים |
| QA-03 | הטופס האנגלי ביקש דרכון בפורמט שהשרת דוחה | `en/intake.html`, `intake.html`, `assets/intake.js` | בדיקה בשתי השפות |
| QA-04 | כשל מייל שמוחזר כערך ולא כחריגה לא נרשם | `api/submit.js` | 3 בדיקות: הצלחה, `error`, חריגה |
| QA-05 | תפריט המובייל לא נסגר בניווט באותו עמוד | `assets/settings.js` | Chromium ‏390×844, לפני ואחרי |
| QA-06 | החיפוש לא מצא את מספר התיק המוצג | `components/crm/CrmApp.tsx` | 2 בדיקות |
| QA-07 | דוח ״7 ימים אחרונים״ לא הוגבל לשבוע | `lib/crm/weekly.ts` (חדש), `store.ts`, `CrmApp.tsx` | 6 בדיקות עם זמן קבוע |
| QA-08 | תזכורת טיסה נשארה אצל האחראי הקודם | `supabase/migrations/202609110006_reminder_ownership.sql` (חדש) | 5 בדיקות מול PostgreSQL מבודד |

### ראיות משמעותיות

**QA-05** שוחזר ותוקן בדפדפן אמיתי ברוחב 390. לפני: `aria-expanded=true`, גלילת הגוף
נעולה, הפאנל מכסה את המקטע, מיקוד ב־`BODY`. אחרי: `aria-expanded=false`, גלילה
משוחררת, הפאנל סגור, מיקוד על `#pricing`.

**QA-08** שוחזר במסד מבודד: בהסרת מיגרציה 006 שלוש מחמש הבדיקות נכשלות ומראות את
האחראי הקודם עדיין בתור למייל ולטלגרם. עם המיגרציה — התור מסומן
`recipient_no_longer_responsible` והתזכורת עוברת לאחראי החדש.

## מצב הבדיקות

`pnpm test` — **69 בדיקות, 67 עברו, 0 נכשלו, 2 דולגו** (בדיקות ה־HTTP, שדורשות
`CRM_TEST_ORIGIN` ושרת מקומי). `pnpm typecheck` נקי, `pnpm build` עובר.

בדיקות האתר רצות כעת באותה חבילה: `crm/package.json` מריץ גם `../tests/*.test.mjs`,
ו־`crm-checks.yml` מאזין גם ל־`assets/**` ול־`tests/**`.

## שינויי מבנה

- `assets/intake-draft.js` — מדיניות הטיוטה, מופרדת כדי שתהיה ניתנת לבדיקה.
- `crm/lib/crm/weekly.ts` — חלון שבעת הימים, מחוץ ל־`store.ts` שהוא `server-only`.
- `tests/` בשורש — בדיקות האתר.

## מה עדיין חסום, ולמה

החסמים מסעיף 6 בדוח ה־QA לא זזו, ואי אפשר להסיר אותם מכאן:

1. **אין Supabase מחובר.** נדרשים פרויקט מורשה, הרצת שש המיגרציות לפי סדר,
   והזמנת מנהל המערכת. הסיסמה נבחרת על ידי הבעלים.
2. **אין Turnstile.** בלי שני המפתחות הקליטה הציבורית חסומה ומחזירה 503. זו חסימה מכוונת.
3. **אין מייל/טלגרם מאומתים ואין cron פעיל.** תיקון QA-04 מתעד כעת כשל משלוח,
   אבל אימות אמיתי דורש חשבון ויעדי בדיקה מאושרים.
4. **בדיקת הרשאות בענן, גיבוי ושחזור, ו־MFA** — לא נבדקו. אין הבטחה לאפס חדירות.

## שאלות פתוחות לבעלים

1. **דרכונים לא ישראליים.** השרת דורש 8 ספרות בכל השפות. יישרתי את הטופס האנגלי
   לכלל הזה במקום להרחיב את השירות בהנחה. אם השירות אמור לקבל דרכונים זרים,
   זו החלטה שלך ואשנה את שני הצדדים יחד.
2. **מיזוג ל־`main`.** ה־CRM נפרס כפרויקט נפרד ותצורת האתר לא נגעה, אבל השינויים
   ב־`api/submit.js` כן מגיעים לאתר החי. הם מהודקים יותר מקודם ומכוסים בבדיקות;
   הגשר עצמו נשאר כבוי ללא משתני סביבה.
3. **QA-09 (תצוגת סלפי מול CSP).** נשאר חשד לא מאומת. דורש בדיקה בדפדפני היעד
   עם כותרות הפריסה בפועל; לא הרחבתי את ה־CSP ללא צורך מוכח.
4. **`noindex` באתר.** צריך לברר אם זו בחירת טרום־השקה.

## הערה על תחזוקה

`components/crm/CrmApp.tsx` הוא 2,515 שורות בקומפוננטה אחת, ו־`lib/crm/store.ts`
הוא 797 שורות. זה עובד, אבל זו הנקודה שתכאב בתחזוקה. פירוק הדרגתי אפשרי
בלי כתיבה מחדש; לא ביצעתי אותו כדי לא לערבב שינוי מבנה בתיקוני באגים.
