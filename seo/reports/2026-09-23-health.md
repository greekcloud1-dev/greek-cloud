# R1 · Daily health and drift — 2026-09-23

**Verdict: clean.** First-run drift baseline captured; nothing else found.

---

## 1. Checks

```
$ node scripts/check-seo.mjs

404.html
   warn  title                  24 chars, under 25 — likely thin

terms.html
   warn  title                  23 chars, under 25 — likely thin

0 error(s), 2 warning(s) across 2 page(s).
```

Both warnings are pre-existing (title length only) and outside today's scope —
not new, not touched.

```
$ node scripts/check-duplication.mjs

11 destination pages under /

highest pairwise overlap:
   32%  rhodes / kos  (16 sentences)
   32%  rhodes / corfu  (16 sentences)
   31%  athens / thessaloniki  (12 sentences)
   31%  thessaloniki / crete  (12 sentences)
   31%  thessaloniki / rhodes  (12 sentences)
   31%  thessaloniki / kos  (12 sentences)

  worst 32%, median 27%, threshold 45%

no block of 35+ words appears on more than 5 pages.

clean.
```

```
$ node scripts/check-duplication.mjs --en

10 destination pages under en/

highest pairwise overlap:
   33%  athens / thessaloniki  (13 sentences)
   33%  athens / rhodes  (13 sentences)
   33%  athens / mykonos  (13 sentences)
   33%  thessaloniki / crete  (13 sentences)
   33%  thessaloniki / rhodes  (13 sentences)
   33%  thessaloniki / kos  (13 sentences)

  worst 33%, median 33%, threshold 45%

no block of 35+ words appears on more than 5 pages.

clean.
```

```
$ node scripts/build-sitemap.mjs --dry-run
sitemap.xml already current — 57 URLs.
```

`npm run check` and `npm run check:dup` both pass (same output as above, via
npm). Ran `npm test` as an extra sanity check, outside R1's required gates:
21/24 pass; the 3 failures (`crm-api`, `form-contract`, `submit`) all abort on
`ERR_MODULE_NOT_FOUND: @vercel/blob`, because `node_modules` isn't installed in
this environment and the runbook says never to run `npm install`. That's an
environment artifact, not a code regression — nothing in this diff touches
those modules, and the failure is import-time, before any assertion runs. Not
treated as a finding; flagging for visibility only.

## 2. Live-site guard

| URL | Status | Notes |
|---|---|---|
| `https://greek-cloud.com/` | 200 | — |
| `https://greek-cloud.com/athens.html` | 200 | no `X-Robots-Tag` header present |
| `https://greek-cloud.com/robots.txt` | 200 | `Allow: /` for `*`, `GPTBot`, `ClaudeBot`, `PerplexityBot`, `OAI-SearchBot`, `Google-Extended`; `Sitemap:` line present |
| `https://greek-cloud.com/sitemap.xml` | 200 | — |
| `https://greek-cloud.com/657aba77224ced8ed594032ef5f33f71.txt` | 200 | IndexNow key, 33 bytes |

No sitewide noindex. AI-crawler allowances intact. All clear.

## 3. Drift

`seo/state/drift/` did not exist — this is the first run.

The `seo-drift` skill's own backend (`~/.claude/skills/seo/bin/claude-seo`,
SQLite at `~/.cache/claude-seo/`) is not present in this environment, and its
default storage wouldn't survive a scheduled routine anyway (each run gets a
fresh container; only what's committed to the repo persists). So the baseline
was captured directly from the repo's own HTML — which is exactly what's
deployed — using a new dependency-free script, `scripts/drift-snapshot.mjs`,
built on the same `scripts/lib/pages.mjs` helpers `check-seo.mjs` already uses.

Captured for all 57 indexable pages (31 Hebrew + 26 English city/content pages,
`404.html`/`intake.html`/`thank-you.html` and their `/en/` twins excluded as
deliberately noindex): title, meta description, canonical, hreflang set, H1s,
JSON-LD `@type` list and node count. Written to `seo/state/drift/baseline.json`.

No comparison to report — there is nothing to compare against yet. Tomorrow's
R1 run should diff a fresh `node scripts/drift-snapshot.mjs` output against
this file.

## 4. What changed

Added, not touched anything live-facing:
- `scripts/drift-snapshot.mjs` — new script, `scripts/` only
- `seo/state/drift/baseline.json`, `seo/state/drift/README.md` — new state

Both are excluded from the Vercel deployment (`.vercelignore` covers `scripts/`
and `seo/`). No visitor-facing change.

Left alone: the two pre-existing title-length warnings (`404.html`,
`terms.html`) — cosmetic, and fixing wording is out of scope for a mechanical
drift-baseline run per the runbook's own table (touches page content, not a
mechanical fix).
